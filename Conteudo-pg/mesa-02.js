// Concurso de Stand up composto por 3 apresentações por participantes

const alicia = [23, 82, 46];
const bob = [45, 8, 32];

function encontrarGanhador(a, b){
  let pontosParticipanteA = 0; 
  let pontosParticipanteB = 0;
  
  for ( i = 0; i <= (array.length-1); i++) {

    if (a[i] > b[i]){

        alice = pontosParticipanteA.push(i);

      } else if (a[i] < b[i]) {

        bob = pontosParticipanteB.push(i);
      } else {
          console.log('Deu empate');
      }

      // console.log(pontosParticipanteA);
  }

  
}

// console.log('O ganhador é: ' );